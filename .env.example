# Copy to .env before running docker compose.

# Port nginx listens on — point your reverse proxy here.
# Caddy:  reverse_proxy localhost:8091
PORT=8091

# PostgreSQL password — must match hibernate.connection.password in config/piped.properties
POSTGRES_PASSWORD=changeme
